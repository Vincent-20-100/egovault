# Agent drafts — WIP only, never committed
.agents/drafts/*
!.agents/drafts/.gitkeep

# Ruff & Test caches
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
*.egg-info/

# Tests
.pytest_cache/

# Tools
config.yaml
.env*

# Config v2 — gitignored (contain secrets and local paths)
config/user.yaml
config/install.yaml

# User data v2 — lives in egovault-user/, never in this repo
egovault-user/

# OS
.DS_Store
Thumbs.db

# User data — everything belongs to the vault, nothing here
notes/
sources/
_status.md
_index.md
.worktrees/
